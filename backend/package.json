{
  "name": "patientrecords-backend-stub",
  "version": "0.1.0",
  "description": "Minimal backend auth stub for PatientRecords (login/refresh/health)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --coverage --runInBand"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "jsonwebtoken": "^9.0.0",
    "swagger-ui-express": "^4.6.3",
    "mongoose": "^7.5.0"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "../coverage/backend"
  }
}
