services:
  # Shell App (Host) - Orchestrator
  patientrecord-shell:
    build:
      context: ./frontend
      dockerfile: shell-app/Dockerfile
    image: patientrecord-shell
    container_name: patientrecord-shell
    ports:
      - "4200:4200"
    restart: unless-stopped
    depends_on:
      patientrecord-api-gateway:
        condition: service_healthy
    networks:
      - patientrecords

  # Demographics Micro-Frontend (Independent Container)
  patientrecord-demographics:
    build:
      context: ./frontend
      dockerfile: modules/demographics/Dockerfile
    image: patientrecord-demographics
    container_name: patientrecord-demographics
    ports:
      - "4201:4200"
    restart: unless-stopped
    depends_on:
      patientrecord-api-gateway:
        condition: service_healthy
    networks:
      - patientrecords

  # Vitals Micro-Frontend (Independent Container)
  patientrecord-vitals:
    build:
      context: ./frontend
      dockerfile: modules/vitals/Dockerfile
    image: patientrecord-vitals
    container_name: patientrecord-vitals
    ports:
      - "4202:4200"
    restart: unless-stopped
    depends_on:
      patientrecord-api-gateway:
        condition: service_healthy
    networks:
      - patientrecords

  # Labs Micro-Frontend (Independent Container)
  patientrecord-labs:
    build:
      context: ./frontend
      dockerfile: modules/labs/Dockerfile
    image: patientrecord-labs
    container_name: patientrecord-labs
    ports:
      - "4203:4200"
    restart: unless-stopped
    depends_on:
      patientrecord-api-gateway:
        condition: service_healthy
    networks:
      - patientrecords

  # Medications Micro-Frontend (Independent Container)
  patientrecord-medications:
    build:
      context: ./frontend
      dockerfile: modules/medications/Dockerfile
    image: patientrecord-medications
    container_name: patientrecord-medications
    ports:
      - "4204:4200"
    restart: unless-stopped
    depends_on:
      patientrecord-api-gateway:
        condition: service_healthy
    networks:
      - patientrecords

  # Visits Micro-Frontend (Independent Container)
  patientrecord-visits:
    build:
      context: ./frontend
      dockerfile: modules/visits/Dockerfile
    image: patientrecord-visits
    container_name: patientrecord-visits
    ports:
      - "4205:4200"
    restart: unless-stopped
    depends_on:
      patientrecord-api-gateway:
        condition: service_healthy
    networks:
      - patientrecords

  # Care Team Micro-Frontend (Independent Container)
  patientrecord-care-team:
    build:
      context: ./frontend
      dockerfile: modules/care-team/Dockerfile
    image: patientrecord-care-team
    container_name: patientrecord-care-team
    ports:
      - "4206:4200"
    restart: unless-stopped
    depends_on:
      patientrecord-api-gateway:
        condition: service_healthy
    networks:
      - patientrecords

  patientrecord-procedures:
    build:
      context: ./frontend/modules/procedures-react
      dockerfile: Dockerfile
    image: patientrecord-procedures
    container_name: patientrecord-procedures
    ports:
      - "4207:4200"
    restart: unless-stopped
    depends_on:
      patientrecord-api-gateway:
        condition: service_healthy
    networks:
      - patientrecords

  patientrecord-api-gateway:
    build:
      context: ./backend/services/api-gateway
      dockerfile: Dockerfile
    image: patientrecord-api-gateway
    container_name: patientrecord-api-gateway
    environment:
      - PORT=5000
      - AUTH_SERVICE_URL=http://patientrecord-auth-service:5001
      - PATIENT_SERVICE_URL=http://patientrecord-patient-service:5002
      - REGISTRY_SERVICE_URL=http://patientrecord-registry-service:5100
      - VITALS_SERVICE_URL=http://patientrecord-vitals-service:5003
      - LABS_SERVICE_URL=http://patientrecord-labs-service:5004
      - MEDICATIONS_SERVICE_URL=http://patientrecord-medications-service:5005
      - VISITS_SERVICE_URL=http://patientrecord-visits-service:5006
      - CARE_TEAM_SERVICE_URL=http://patientrecord-care-team-service:5007
    ports:
      - "5000:5000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      patientrecord-auth-service:
        condition: service_healthy
      patientrecord-patient-service:
        condition: service_healthy
      patientrecord-registry-service:
        condition: service_healthy
      patientrecord-vitals-service:
        condition: service_healthy
      patientrecord-labs-service:
        condition: service_healthy
      patientrecord-medications-service:
        condition: service_healthy
      patientrecord-visits-service:
        condition: service_healthy
      patientrecord-care-team-service:
        condition: service_healthy
    networks:
      - patientrecords

  patientrecord-auth-service:
    build:
      context: ./backend/services/auth-service
      dockerfile: Dockerfile
    image: patientrecord-auth-service
    container_name: patientrecord-auth-service
    environment:
      - PORT=5001
      - JWT_SECRET=${JWT_SECRET:-dev-secret}
      - TOKEN_EXPIRATION_MINUTES=${TOKEN_EXPIRATION_MINUTES:-60}
    ports:
      - "5001:5001"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5001/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - patientrecords

  patientrecord-patient-service:
    build:
      context: ./backend/services/patient-service
      dockerfile: Dockerfile
    image: patientrecord-patient-service
    container_name: patientrecord-patient-service
    environment:
      - PORT=5002
      - JWT_SECRET=${JWT_SECRET:-dev-secret}
      - MONGODB_URI=mongodb://admin:admin@patientrecord-mongodb:27017/patientrecords?authSource=admin
    ports:
      - "5002:5002"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5002/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      patientrecord-mongodb:
        condition: service_healthy
    networks:
      - patientrecords

  patientrecord-vitals-service:
    build:
      context: ./backend
      dockerfile: services/vitals-service/Dockerfile
    image: patientrecord-vitals-service
    container_name: patientrecord-vitals-service
    environment:
      - PORT=5003
      - JWT_SECRET=${JWT_SECRET:-dev-secret}
      - MONGODB_URI=mongodb://admin:admin@patientrecord-mongodb:27017/patientrecords?authSource=admin
      - REDIS_HOST=patientrecord-redis
      - REDIS_PORT=6379
    ports:
      - "5003:5003"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5003/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      patientrecord-mongodb:
        condition: service_healthy
      patientrecord-redis:
        condition: service_healthy
    networks:
      - patientrecords

  patientrecord-labs-service:
    build:
      context: ./backend
      dockerfile: services/labs-service/Dockerfile
    image: patientrecord-labs-service
    container_name: patientrecord-labs-service
    environment:
      - PORT=5004
      - JWT_SECRET=${JWT_SECRET:-dev-secret}
      - MONGODB_URI=mongodb://admin:admin@patientrecord-mongodb:27017/patientrecords?authSource=admin
      - REDIS_HOST=patientrecord-redis
      - REDIS_PORT=6379
    ports:
      - "5004:5004"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5004/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      patientrecord-mongodb:
        condition: service_healthy
      patientrecord-redis:
        condition: service_healthy
    networks:
      - patientrecords

  patientrecord-medications-service:
    build:
      context: ./backend
      dockerfile: services/medications-service/Dockerfile
    image: patientrecord-medications-service
    container_name: patientrecord-medications-service
    environment:
      - PORT=5005
      - JWT_SECRET=${JWT_SECRET:-dev-secret}
      - MONGODB_URI=mongodb://admin:admin@patientrecord-mongodb:27017/patientrecords?authSource=admin
      - REDIS_HOST=patientrecord-redis
      - REDIS_PORT=6379
    ports:
      - "5005:5005"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5005/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      patientrecord-mongodb:
        condition: service_healthy
      patientrecord-redis:
        condition: service_healthy
    networks:
      - patientrecords

  patientrecord-visits-service:
    build:
      context: ./backend
      dockerfile: services/visits-service/Dockerfile
    image: patientrecord-visits-service
    container_name: patientrecord-visits-service
    environment:
      - PORT=5006
      - JWT_SECRET=${JWT_SECRET:-dev-secret}
      - MONGODB_URI=mongodb://admin:admin@patientrecord-mongodb:27017/patientrecords?authSource=admin
      - REDIS_HOST=patientrecord-redis
      - REDIS_PORT=6379
    ports:
      - "5006:5006"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5006/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      patientrecord-mongodb:
        condition: service_healthy
      patientrecord-redis:
        condition: service_healthy
    networks:
      - patientrecords

  patientrecord-care-team-service:
    build:
      context: ./backend
      dockerfile: services/care-team-service/Dockerfile
    image: patientrecord-care-team-service
    container_name: patientrecord-care-team-service
    environment:
      - PORT=5007
      - JWT_SECRET=${JWT_SECRET:-dev-secret}
      - MONGODB_URI=mongodb://admin:admin@patientrecord-mongodb:27017/patientrecords?authSource=admin
      - REDIS_HOST=patientrecord-redis
      - REDIS_PORT=6379
    ports:
      - "5007:5007"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5007/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      patientrecord-mongodb:
        condition: service_healthy
      patientrecord-redis:
        condition: service_healthy
    networks:
      - patientrecords

  patientrecord-registry-service:
    build:
      context: ./backend/services/registry-service
      dockerfile: Dockerfile
    image: patientrecord-registry-service
    container_name: patientrecord-registry-service
    environment:
      - PORT=5100
      - JWT_SECRET=${JWT_SECRET:-dev-secret}
      - MONGODB_URI=mongodb://admin:admin@patientrecord-mongodb:27017/patientrecords?authSource=admin
    ports:
      - "5100:5100"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5100/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      patientrecord-mongodb:
        condition: service_healthy
    networks:
      - patientrecords

  patientrecord-redis:
    build:
      context: ./redis
      dockerfile: Dockerfile
    image: patientrecord-redis
    container_name: patientrecord-redis
    command: redis-server --appendonly yes --appendfsync everysec
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped
    networks:
      - patientrecords

  # RedisInsight — browser UI for Redis (dev only) → http://localhost:5540
  patientrecord-redisinsight:
    build:
      context: ./redisinsight
      dockerfile: Dockerfile
    image: patientrecord-redisinsight
    container_name: patientrecord-redisinsight
    ports:
      - "5540:5540"
    depends_on:
      patientrecord-redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - patientrecords

  patientrecord-mongodb:
    build:
      context: ./mongo
      dockerfile: Dockerfile
    image: patientrecord-mongodb
    container_name: patientrecord-mongo
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=admin
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')", "-u", "admin", "-p", "admin"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - patientrecords

volumes:
  mongo_data:
    driver: local
  redis-data:
    driver: local

networks:
  patientrecords:
    driver: bridge
